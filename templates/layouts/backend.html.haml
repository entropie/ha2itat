!!!
%html{:lang => "en"}<
  %head<
    %meta{charset: "utf-8"}
    - title = "ze backend yO?"
    %title= title
    %meta{name: "title", content: "#{title}"}
    %link{ href: url_with_calculated_version_hash("/assets/favicon.ico"), rel: "shortcut icon", type: "image/x-icon" }
    - if Hanami.env?(:production)
      %link{href: url_with_calculated_version_hash("/assets/screen-cm.css"), rel: 'stylesheet', type: 'text/css'}

  %body#body-backend<
    .backend-block

      = render "shared/menu"
      = render "submenu" rescue nil
      .backend-content
        = yield

  %script{:src => url_with_calculated_version_hash("/assets/bundle-be.js")}
