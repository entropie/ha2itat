%h1= t.be.topics.tumblog.edit

.backend-page
  = render "form", action: path(:backend_tumblog_edit, id: post.id), post: post

%hr
.backend-content.tumblog
  %article.snippet-content{class: post.css_class}
    = post.to_html(true)
