%ul
  %li.imp>= nlink(path(:backend_tumblog_show, id: post.id), t.icons.show("be.links.tumblog.show"), title: t.be.links.tumblog.show)
  %li>= nlink(path(:backend_tumblog_toggleprivate, id: post.id, goto: current_uri), (post.private? ? t.icons.publish("be.links.tumblog.publish") : t.icons.unpublish("be.links.tumblog.unpublish")), title: (post.private? ? t.be.links.tumblog.publish : t.be.links.tumblog.unpublish ), class: "publish-toggle-link")
  %li>= nlink(path(:backend_tumblog_edit, id: post.id), t.icons.edit("be.links.tumblog.edit"), title: t.be.links.tumblog.edit)
  %li.warn>= nlink(path(:backend_tumblog_destroy, id: post.id, goto: current_uri), t.icons.destroy("be.links.tumblog.destroy"), onclick: "return confirm('%s')" % t.be.ask.destroy)

